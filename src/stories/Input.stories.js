import Navigation from '../components/Navigation';

const stories = {
  title: 'Input',
  component: Navigation,
};

export default stories;

const Default = () => (
  <Navigation
    title="input"
  />
);

export { Default };
